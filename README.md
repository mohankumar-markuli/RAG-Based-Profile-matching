# RAG-Based-Profile-matching
A Resume RAG system that processes resumes using intelligent chunking, generates embeddings, and stores them in a vector database. It performs semantic and keyword-based job matching, ranks candidates with match scores, extracts metadata, and provides explainable hiring recommendations.
